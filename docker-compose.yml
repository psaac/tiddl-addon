version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8765:8765"
    environment:
      - PYTHONUNBUFFERED=1
      - BACKEND_HOST=0.0.0.0
      - BACKEND_PORT=8765
    volumes:
      - ~/.tiddl:/root/.tiddl # Mount tiddl config directory
    restart: unless-stopped
